{% set name = "pyrosar" %}
{% set version = "0.38.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: df557b241250e77d7ac46c598dd30786862c6f08d2b8f5fe4358f8970d1044fb

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - python >=3.11
    - pip
    - setuptools
    - setuptools_scm
  run:
    - gdal >=2.4
    - geoalchemy2 >=0.14.0
    - libgdal
    - libspatialite >=5.1.0
    - lxml
    - numpy
    - packaging
    - pillow
    - progressbar2
    - psutil
    - psycopg2
    - python >=3.11
    - pyyaml
    - requests
    - spatialist >=0.17.0
    - sqlalchemy >=1.4
    - sqlalchemy-utils >=0.37,<0.42

test:
  imports:
    - pyroSAR
  commands:
    - pip check
  requires:
    - python >=3.11
    - pip

about:
  home: https://github.com/johntruckenbrodt/pyroSAR
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: A Python Framework for Large-Scale SAR Satellite Data Processing

  description: |
    The pyroSAR package aims at providing a complete solution for the scalable organization and processing of SAR satellite data.
  doc_url: https://pyrosar.readthedocs.io
  dev_url: https://github.com/johntruckenbrodt/pyroSAR

extra:
  recipe-maintainers:
    - johntruckenbrodt
